/**
 * requery config file
 * if you need update config path , please modification BaseUrl.
 * Created by zxb on 2015/6/3.
 */

(function () {
    require.config({
        baseUrl: "../src/",
        waitSeconds: 15,
        paths: {
            "jquery": "jquery"
        }
    });
})();
